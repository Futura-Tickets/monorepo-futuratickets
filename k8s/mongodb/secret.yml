apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: futuratickets
type: Opaque
stringData:
  # IMPORTANT: Replace these with your actual credentials
  # Use: kubectl create secret generic mongodb-secret --from-literal=username=admin --from-literal=password=YOUR_PASSWORD
  username: admin
  password: CHANGE_ME_IN_PRODUCTION
