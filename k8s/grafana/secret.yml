apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: futuratickets
type: Opaque
stringData:
  # IMPORTANT: Replace with strong credentials
  # Use: kubectl create secret generic grafana-secret --from-literal=admin-user=admin --from-literal=admin-password=YOUR_PASSWORD
  admin-user: admin
  admin-password: CHANGE_ME_IN_PRODUCTION
