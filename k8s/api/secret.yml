apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: futuratickets
type: Opaque
stringData:
  # IMPORTANT: Replace all values with production credentials
  # Create using: kubectl create secret generic api-secret --from-env-file=.env.production

  mongo-url: "mongodb://admin:CHANGE_ME@mongodb:27017/futura-tickets?authSource=admin"
  jwt-secret: "CHANGE_ME_STRONG_SECRET"
  stripe-secret-key: "sk_live_CHANGE_ME"
  stripe-public-key: "pk_live_CHANGE_ME"
  stripe-webhook-secret: "whsec_CHANGE_ME"
  azure-storage-connection: "DefaultEndpointsProtocol=https;AccountName=CHANGE_ME;AccountKey=CHANGE_ME"
  sentry-dsn: "https://CHANGE_ME@sentry.io/PROJECT_ID"
